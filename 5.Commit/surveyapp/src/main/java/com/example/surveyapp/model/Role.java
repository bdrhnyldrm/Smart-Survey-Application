package com.example.surveyapp.model;

/*Enum (kısa adıyla "enumeration"), sabit değerlerin (constant) isimlendirilmiş bir listesini
tanımlamak için kullanılan bir veri türüdür.*/


public enum Role {
    ADMIN,
    CUSTOMER
}
